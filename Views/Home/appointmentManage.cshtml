
@{

    Layout = "~/Views/Shared/layout.cshtml";
}
<!--session 39: table for receptionist-->
<div class="panel panel-primary filterable">
    <div class="panel-heading">
        <h3 class="panel-title">Users</h3>
        <div class="pull-left">
            <button class="btn btn-default btn-xs btn-filter"><span class="fa fa-filter"></span> فیلتر </button>
            <button class="btn btn-info btn-xs" onclick="addPrmtr()"><span class="fa fa-plus"></span> اضافه کردن نوبت </button>
        </div>
    </div>
    <table class="table">
        <thead>
            <tr class="filters">
                <th><input type="text" class="form-control" placeholder="نوبت" disabled></th>
                <th><input type="text" class="form-control" placeholder="پزشک" disabled></th>
                <th><input type="text" class="form-control" placeholder="تاریخ" disabled></th>
                <th><input type="text" class="form-control" placeholder="ساعت" disabled></th>
                <th><input type="text" class="form-control" placeholder="اطلاعات بیمار" disabled></th>
                <th><input type="text" class="form-control" placeholder="شماره ملی" disabled></th>
                <th><input type="text" class="form-control" placeholder="شماره تماس بیمار" disabled></th>
                <th><input type="text" class="form-control" placeholder="نوع" disabled></th>
                <th><input type="text" class="form-control" placeholder="وضعیت" disabled></th>
            </tr>
        </thead>
        <tbody id="tblAppointment">
        </tbody>
    </table>
</div>

@section script{

    this is at. section is because of preventing executing js code in the middle of running
    as you can see this is the content that is run whene ever cilient clicks on "مدیریت نوبت ها"
    , thus this content is the middle of other running
    <script src="~/assets/js/appointment.js"></script>

}
